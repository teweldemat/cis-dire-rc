# Remote Control React Frontend

## Development

```bash
cd deploy/site-config/dire/remot-control/react-frontend
npm install
npm run dev
```

## Build

```bash
npm run build
```

The static output is generated in `dist/` and should be served by Nginx.
